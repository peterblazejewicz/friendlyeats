{
  "name": "friendlyeats-blazejewicz",
  "version": "0.0.0",
  "description": "Starting point for the Cloud Firestore codelab",
  "main": ".",
  "scripts": {
    "fmt": "prettier --write scripts/* styles/*",
    "build:tsc": "tsc -p ./tsconfig.json",
    "build:tsc:w": "tsc -p ./tsconfig.json -w"
  },
  "author": "",
  "devDependencies": {
    "@types/navigo": "^7.0.1",
    "eslint": "^4.17.0",
    "firebase": "^5.5.5",
    "prettier": "^1.5.3",
    "typescript": "^3.1.3"
  }
}
